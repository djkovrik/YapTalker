<resources>

  <!-- BASE THEME (LIGHT) -->
  <style name="AppTheme" parent="MaterialDrawerTheme.Light.DarkToolbar">
    <item name="colorPrimary">@color/colorPrimary</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    <item name="colorAccent">@color/colorAccent</item>
    <item name="android:windowBackground">@color/colorMaterialBackground</item>
    <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Material</item>
  </style>

  <!-- DARK THEME -->
  <style name="AppTheme.Dark" parent="MaterialDrawerTheme.ActionBar">
    <item name="colorPrimary">@color/colorPrimaryD</item>
    <item name="colorPrimaryDark">@color/colorPrimaryDarkD</item>
    <item name="colorAccent">@color/colorAccentD</item>
    <item name="android:windowBackground">@color/colorMaterialBackgroundD</item>
    <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Material</item>
  </style>

  <style name="AppTheme.NoActionBar">
    <item name="windowActionBar">false</item>
    <item name="windowNoTitle">true</item>
  </style>

  <style name="AppTheme.Settings">
    <item name="windowActionBar">true</item>
    <item name="windowNoTitle">false</item>
  </style>

  <!-- Video activity theme -->
  <style name="AppTheme.DarkBackground" parent="AppTheme.NoActionBar">
    <item name="android:windowBackground">@color/material_color_black</item>
  </style>

  <!-- Overlays -->
  <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
  <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
