package kotlinx.android.synthetic.main.fragment_site_search

import com.sedsoftware.yaptalker.R
import com.sedsoftware.yaptalker.synthetic.syntheticView
import android.app.Activity
import androidx.fragment.app.Fragment
import android.view.View
import android.widget.Button
import android.widget.CheckBox
import android.widget.EditText
import android.widget.RadioButton
import android.widget.RadioGroup
import android.widget.Spinner
import android.widget.TextView

val Activity.search_target_text: EditText
    get() = syntheticView(R.id.search_target_text)

val Fragment.search_target_text: EditText
    get() = syntheticView(R.id.search_target_text)

val View.search_target_text: EditText
    get() = syntheticView(R.id.search_target_text)

val Activity.search_button: Button
    get() = syntheticView(R.id.search_button)

val Fragment.search_button: Button
    get() = syntheticView(R.id.search_button)

val View.search_button: Button
    get() = syntheticView(R.id.search_button)

val Activity.search_group_sorting: RadioGroup
    get() = syntheticView(R.id.search_group_sorting)

val Fragment.search_group_sorting: RadioGroup
    get() = syntheticView(R.id.search_group_sorting)

val View.search_group_sorting: RadioGroup
    get() = syntheticView(R.id.search_group_sorting)

val Activity.search_rb_date: RadioButton
    get() = syntheticView(R.id.search_rb_date)

val Fragment.search_rb_date: RadioButton
    get() = syntheticView(R.id.search_rb_date)

val View.search_rb_date: RadioButton
    get() = syntheticView(R.id.search_rb_date)

val Activity.search_rb_relevant: RadioButton
    get() = syntheticView(R.id.search_rb_relevant)

val Fragment.search_rb_relevant: RadioButton
    get() = syntheticView(R.id.search_rb_relevant)

val View.search_rb_relevant: RadioButton
    get() = syntheticView(R.id.search_rb_relevant)

val Activity.textView: TextView
    get() = syntheticView(R.id.textView)

val Fragment.textView: TextView
    get() = syntheticView(R.id.textView)

val View.textView: TextView
    get() = syntheticView(R.id.textView)

val Activity.textView2: TextView
    get() = syntheticView(R.id.textView2)

val Fragment.textView2: TextView
    get() = syntheticView(R.id.textView2)

val View.textView2: TextView
    get() = syntheticView(R.id.textView2)

val Activity.search_group_conditions: RadioGroup
    get() = syntheticView(R.id.search_group_conditions)

val Fragment.search_group_conditions: RadioGroup
    get() = syntheticView(R.id.search_group_conditions)

val View.search_group_conditions: RadioGroup
    get() = syntheticView(R.id.search_group_conditions)

val Activity.search_rb_words_any: RadioButton
    get() = syntheticView(R.id.search_rb_words_any)

val Fragment.search_rb_words_any: RadioButton
    get() = syntheticView(R.id.search_rb_words_any)

val View.search_rb_words_any: RadioButton
    get() = syntheticView(R.id.search_rb_words_any)

val Activity.search_rb_words_all: RadioButton
    get() = syntheticView(R.id.search_rb_words_all)

val Fragment.search_rb_words_all: RadioButton
    get() = syntheticView(R.id.search_rb_words_all)

val View.search_rb_words_all: RadioButton
    get() = syntheticView(R.id.search_rb_words_all)

val Activity.search_rb_words_phrase: RadioButton
    get() = syntheticView(R.id.search_rb_words_phrase)

val Fragment.search_rb_words_phrase: RadioButton
    get() = syntheticView(R.id.search_rb_words_phrase)

val View.search_rb_words_phrase: RadioButton
    get() = syntheticView(R.id.search_rb_words_phrase)

val Activity.textView3: TextView
    get() = syntheticView(R.id.textView3)

val Fragment.textView3: TextView
    get() = syntheticView(R.id.textView3)

val View.textView3: TextView
    get() = syntheticView(R.id.textView3)

val Activity.search_period_spinner: Spinner
    get() = syntheticView(R.id.search_period_spinner)

val Fragment.search_period_spinner: Spinner
    get() = syntheticView(R.id.search_period_spinner)

val View.search_period_spinner: Spinner
    get() = syntheticView(R.id.search_period_spinner)

val Activity.search_box_feed: CheckBox
    get() = syntheticView(R.id.search_box_feed)

val Fragment.search_box_feed: CheckBox
    get() = syntheticView(R.id.search_box_feed)

val View.search_box_feed: CheckBox
    get() = syntheticView(R.id.search_box_feed)

val Activity.search_box_communication: CheckBox
    get() = syntheticView(R.id.search_box_communication)

val Fragment.search_box_communication: CheckBox
    get() = syntheticView(R.id.search_box_communication)

val View.search_box_communication: CheckBox
    get() = syntheticView(R.id.search_box_communication)

val Activity.search_box_chaos: CheckBox
    get() = syntheticView(R.id.search_box_chaos)

val Fragment.search_box_chaos: CheckBox
    get() = syntheticView(R.id.search_box_chaos)

val View.search_box_chaos: CheckBox
    get() = syntheticView(R.id.search_box_chaos)
