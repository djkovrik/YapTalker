package kotlinx.android.synthetic.main.fragment_user_profile

import com.sedsoftware.yaptalker.R
import com.sedsoftware.yaptalker.synthetic.syntheticView
import android.app.Activity
import androidx.fragment.app.Fragment
import android.view.View
import android.widget.ImageView
import android.widget.TextView
import androidx.cardview.widget.CardView

val Activity.profile_group: TextView
    get() = syntheticView(R.id.profile_group)

val Fragment.profile_group: TextView
    get() = syntheticView(R.id.profile_group)

val View.profile_group: TextView
    get() = syntheticView(R.id.profile_group)

val Activity.profile_status: TextView
    get() = syntheticView(R.id.profile_status)

val Fragment.profile_status: TextView
    get() = syntheticView(R.id.profile_status)

val View.profile_status: TextView
    get() = syntheticView(R.id.profile_status)

val Activity.profile_uq: TextView
    get() = syntheticView(R.id.profile_uq)

val Fragment.profile_uq: TextView
    get() = syntheticView(R.id.profile_uq)

val View.profile_uq: TextView
    get() = syntheticView(R.id.profile_uq)

val Activity.profile_sign: TextView
    get() = syntheticView(R.id.profile_sign)

val Fragment.profile_sign: TextView
    get() = syntheticView(R.id.profile_sign)

val View.profile_sign: TextView
    get() = syntheticView(R.id.profile_sign)

val Activity.profile_rewards: TextView
    get() = syntheticView(R.id.profile_rewards)

val Fragment.profile_rewards: TextView
    get() = syntheticView(R.id.profile_rewards)

val View.profile_rewards: TextView
    get() = syntheticView(R.id.profile_rewards)

val Activity.profile_registered: TextView
    get() = syntheticView(R.id.profile_registered)

val Fragment.profile_registered: TextView
    get() = syntheticView(R.id.profile_registered)

val View.profile_registered: TextView
    get() = syntheticView(R.id.profile_registered)

val Activity.profile_time_zone: TextView
    get() = syntheticView(R.id.profile_time_zone)

val Fragment.profile_time_zone: TextView
    get() = syntheticView(R.id.profile_time_zone)

val View.profile_time_zone: TextView
    get() = syntheticView(R.id.profile_time_zone)

val Activity.profile_birth_date: TextView
    get() = syntheticView(R.id.profile_birth_date)

val Fragment.profile_birth_date: TextView
    get() = syntheticView(R.id.profile_birth_date)

val View.profile_birth_date: TextView
    get() = syntheticView(R.id.profile_birth_date)

val Activity.profile_web_site: TextView
    get() = syntheticView(R.id.profile_web_site)

val Fragment.profile_web_site: TextView
    get() = syntheticView(R.id.profile_web_site)

val View.profile_web_site: TextView
    get() = syntheticView(R.id.profile_web_site)

val Activity.profile_location: TextView
    get() = syntheticView(R.id.profile_location)

val Fragment.profile_location: TextView
    get() = syntheticView(R.id.profile_location)

val View.profile_location: TextView
    get() = syntheticView(R.id.profile_location)

val Activity.profile_sex: TextView
    get() = syntheticView(R.id.profile_sex)

val Fragment.profile_sex: TextView
    get() = syntheticView(R.id.profile_sex)

val View.profile_sex: TextView
    get() = syntheticView(R.id.profile_sex)

val Activity.profile_interests: TextView
    get() = syntheticView(R.id.profile_interests)

val Fragment.profile_interests: TextView
    get() = syntheticView(R.id.profile_interests)

val View.profile_interests: TextView
    get() = syntheticView(R.id.profile_interests)

val Activity.profile_messages: TextView
    get() = syntheticView(R.id.profile_messages)

val Fragment.profile_messages: TextView
    get() = syntheticView(R.id.profile_messages)

val View.profile_messages: TextView
    get() = syntheticView(R.id.profile_messages)

val Activity.profile_messages_day: TextView
    get() = syntheticView(R.id.profile_messages_day)

val Fragment.profile_messages_day: TextView
    get() = syntheticView(R.id.profile_messages_day)

val View.profile_messages_day: TextView
    get() = syntheticView(R.id.profile_messages_day)

val Activity.profile_bayans: TextView
    get() = syntheticView(R.id.profile_bayans)

val Fragment.profile_bayans: TextView
    get() = syntheticView(R.id.profile_bayans)

val View.profile_bayans: TextView
    get() = syntheticView(R.id.profile_bayans)

val Activity.profile_topics_today: TextView
    get() = syntheticView(R.id.profile_topics_today)

val Fragment.profile_topics_today: TextView
    get() = syntheticView(R.id.profile_topics_today)

val View.profile_topics_today: TextView
    get() = syntheticView(R.id.profile_topics_today)

val Activity.profile_email: TextView
    get() = syntheticView(R.id.profile_email)

val Fragment.profile_email: TextView
    get() = syntheticView(R.id.profile_email)

val View.profile_email: TextView
    get() = syntheticView(R.id.profile_email)

val Activity.profile_icq: TextView
    get() = syntheticView(R.id.profile_icq)

val Fragment.profile_icq: TextView
    get() = syntheticView(R.id.profile_icq)

val View.profile_icq: TextView
    get() = syntheticView(R.id.profile_icq)

val Activity.profile_photo_card: CardView
    get() = syntheticView(R.id.profile_photo_card)

val Fragment.profile_photo_card: CardView
    get() = syntheticView(R.id.profile_photo_card)

val View.profile_photo_card: CardView
    get() = syntheticView(R.id.profile_photo_card)

val Activity.profile_photo: ImageView
    get() = syntheticView(R.id.profile_photo)

val Fragment.profile_photo: ImageView
    get() = syntheticView(R.id.profile_photo)

val View.profile_photo: ImageView
    get() = syntheticView(R.id.profile_photo)
