package kotlinx.android.synthetic.main.fragment_authorization

import com.sedsoftware.yaptalker.R
import com.sedsoftware.yaptalker.synthetic.syntheticView
import android.app.Activity
import androidx.fragment.app.Fragment
import android.view.View
import android.widget.Button
import android.widget.CheckBox
import android.widget.EditText

val Activity.authorization_login: EditText
    get() = syntheticView(R.id.authorization_login)

val Fragment.authorization_login: EditText
    get() = syntheticView(R.id.authorization_login)

val View.authorization_login: EditText
    get() = syntheticView(R.id.authorization_login)

val Activity.authorization_password: EditText
    get() = syntheticView(R.id.authorization_password)

val Fragment.authorization_password: EditText
    get() = syntheticView(R.id.authorization_password)

val View.authorization_password: EditText
    get() = syntheticView(R.id.authorization_password)

val Activity.authorization_remember: CheckBox
    get() = syntheticView(R.id.authorization_remember)

val Fragment.authorization_remember: CheckBox
    get() = syntheticView(R.id.authorization_remember)

val View.authorization_remember: CheckBox
    get() = syntheticView(R.id.authorization_remember)

val Activity.button_sign_in: Button
    get() = syntheticView(R.id.button_sign_in)

val Fragment.button_sign_in: Button
    get() = syntheticView(R.id.button_sign_in)

val View.button_sign_in: Button
    get() = syntheticView(R.id.button_sign_in)
